Suspicious File Lab - Sample Dataset

Use these harmless teaching samples for Bash exercises involving grep/egrep
(grep -E), $?, file operators, conditional constructs, integer comparisons,
and regular expressions.

Directories: backup/ and quarantine/

After extraction, for the permission exercise you may run:
chmod 000 restricted.txt

Run permission tests as a normal user; root can behave differently.
